# Frontend Environment Variables (Safe to expose)
# These are public keys that are safe to include in frontend code

# Supabase Configuration (Frontend only needs public keys)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Backend API Configuration
# For development: http://127.0.0.1:8000
# For production (Railway): https://study-sharper-backend-production.up.railway.app
NEXT_PUBLIC_API_URL=https://study-sharper-backend-production.up.railway.app
NEXT_PUBLIC_WS_URL=wss://study-sharper-backend-production.up.railway.app

# OCR Configuration (Optional - for PDF text extraction fallback)
OCR_SPACE_API_KEY=your_ocr_space_api_key
OCR_SPACE_API_URL=https://api.ocr.space/parse/image
OCR_SPACE_LANGUAGE=eng
OCR_SPACE_ENGINE=2

# ⚠️  CRITICAL SECURITY NOTES ⚠️
# 1. SUPABASE_SERVICE_ROLE_KEY should NEVER be in frontend files - backend only!
# 2. OPENROUTER_API_KEY should NEVER be in frontend files - backend only!
# 3. Only NEXT_PUBLIC_* variables are safe to expose to the browser
# 4. All sensitive API keys belong in backend environment variables
