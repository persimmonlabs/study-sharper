# Frontend Environment Variables (Safe to expose)
# These are public keys that are safe to include in frontend code

# Supabase Configuration (Frontend only needs public keys)
NEXT_PUBLIC_SUPABASE_URL=your_supabase_project_url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your_supabase_anon_key

# Backend API Configuration
# For development: http://127.0.0.1:8000
# For production: your deployed backend URL (e.g., https://your-backend.railway.app)
BACKEND_API_URL=http://127.0.0.1:8000

# OCR Configuration (Optional - for PDF text extraction fallback)
OCR_SPACE_API_KEY=your_ocr_space_api_key
OCR_SPACE_API_URL=https://api.ocr.space/parse/image
OCR_SPACE_LANGUAGE=eng
OCR_SPACE_ENGINE=2

# ⚠️  SECURITY NOTE ⚠️
# The SUPABASE_SERVICE_ROLE_KEY should NEVER be in frontend environment files
# It should only exist in your backend deployment environment
# Frontend only needs the public anon key for user authentication
